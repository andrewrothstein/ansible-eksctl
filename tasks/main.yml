---
- name: resolve platform specific vars
  include_vars: '{{ item }}'
  with_first_found:
    - files:
        - '{{ ansible_distribution }}-{{ ansible_distribution_release }}.yml'
        - '{{ ansible_distribution }}.yml'
        - '{{ ansible_os_family }}.yml'
      skip: true
      paths:
        - '{{ role_path }}/vars'
- name: check for install
  become: yes
  stat:
    path: '{{ eksctl_install_dir }}'
  changed_when: False
  register: eksctl_binary
- when: not eksctl_binary.stat.exists
  block:
    - name: download
      become: yes
      become_user: root
      get_url:
        url: '{{ eksctl_url }}'
        dest: /tmp/{{ eksctl_archive }}
        mode: 0644
        checksum: '{{ eksctl_checksum }}'
    - name: mkdir
      become: yes
      become_user: root
      file:
        path: '{{ eksctl_install_dir }}'
        mode: 0755
        state: directory
    - name: unarchive
      become: yes
      become_user: root
      unarchive:
        remote_src: yes
        src: /tmp/{{ eksctl_archive }}
        dest: '{{ eksctl_install_dir }}'
        creates: '{{ eksctl_install_dir }}/eksctl'
  always:
    - name: cleanup
      become: yes
      become_user: root
      file:
        path: /tmp/{{ eksctl_archive }}
        state: absent
- name: link
  become: yes
  become_user: root
  file:
    src: '{{ eksctl_install_dir }}/eksctl'
    dest: '{{ eksctl_link }}'
    state: link
